{
  "name": "advanced-care-planning-app",
  "version": "1.0.0",
  "description": "BC Advanced Care Planning Guide Assistant - Frontend Only",
  "private": true,
  "scripts": {
    "start": "cd client && npm start",
    "build": "cd client && npm run build && rm -rf ../docs && cp -r client/build ../docs",
    "install": "cd client && npm install"
  }
}
